version: '3.8'
services:
  baro-api:
    build: .                    #  Dockerfile
    ports:
      - "8001:8000"            # Host:Container
    env_file: .env              #  secrets
    restart: unless-stopped     # Auto-restart
